**/target/
**/node_modules/
**/*.rs.bk
*.swp
.wasm-binaries
polkadot/runtime/wasm/target/
core/executor/wasm/target/
core/test-runtime/wasm/target/
pwasm-alloc/target/
pwasm-libc/target/
pwasm-alloc/Cargo.lock
pwasm-libc/Cargo.lock
node/runtime/wasm/target/
**/._*
**/.criterion/
polkadot.*
.DS_Store
.cargo-remote.toml
/artifacts/
tmp/

# Testnet keys — generated secrets, never commit
scripts/testnet-keys/keys/

# Editors
.vscode
.idea/
.zed

# Added by cargo
/target

# Vitepress
/docs/.vitepress/cache
/docs/.vitepress/dist

# NixOS development environment
.envrc

# Node runtime data (chain DB, keystore, network keys)
/data/

# Validator keys
scripts/validator-keys/keys/
docker/testnet/.env

# RPC TLS — Cloudflare Origin Certificate + private key, never commit
docker/testnet/origin.pem
docker/testnet/origin.key

# Generated chain specs (canonical copy lives in node-deploy)
scripts/generate-specs/dist/

# Contiene mnemonics en claro — nunca commitear
scripts/vk/add-validators.cjs

# Vendored crate sources — written by scripts/benchmarks/test-upstream-13073.sh when
# testing an unreleased SDK fix. Several GB, and reproducible with `cargo vendor`.
/vendor/
